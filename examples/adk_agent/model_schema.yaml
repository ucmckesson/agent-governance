input_schema:
  type: object
  properties:
    text:
      type: string
  required: ["text"]
output_schema:
  type: object
  properties:
    text:
      type: string
  required: ["text"]
tool_params:
  search:
    schema:
      type: object
      properties:
        query:
          type: string
      required: ["query"]
